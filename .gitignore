# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Generated files 
gen_models
# *.pb.go TODO find how to generate proto files in docker, we should not push generated files

.env
.vscode

volumes

go.work.sum
book-handler/book-handler
server/server
schemas/old_novel_schema.sql